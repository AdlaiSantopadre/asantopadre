# /home/asantopadre/fabric-2.5/artifacts/fabric-ca-orgx.yaml 23/01
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-ca-orgx-pvc
  namespace: orgx
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
---
apiVersion: v1
kind: Service
metadata:
  name: fabric-ca-orgx
  namespace: orgx
spec:
  selector:
    app: fabric-ca-orgx
  ports:
    - name: https
      port: 7054
      targetPort: 7054
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabric-ca-orgx
  namespace: orgx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabric-ca-orgx
  template:
    metadata:
      labels:
        app: fabric-ca-orgx
    spec:
      containers:
        - name: fabric-ca
          image: hyperledger/fabric-ca:1.5.9
          args:
            - sh
            - -c
            - fabric-ca-server start -b boot:bootpw
          env:
            - name: FABRIC_CA_HOME
              value: /etc/hyperledger/fabric-ca-server
            - name: FABRIC_CA_SERVER_CA_NAME
              value: ca-orgx
            - name: FABRIC_CA_SERVER_TLS_ENABLED
              value: "true"
            - name: FABRIC_CA_SERVER_CSR_CN
              value: fabric-ca-orgx
            - name: FABRIC_CA_SERVER_CSR_HOSTS
              value: fabric-ca-orgx,fabric-ca-orgx.orgx.svc,fabric-ca-orgx.orgx.svc.cluster.local,localhost
            - name: FABRIC_CA_SERVER_CFG_IDENTITIES_ALLOWREMOVE
              value: "true"
          ports:
            - containerPort: 7054
          volumeMounts:
            - name: data
              mountPath: /etc/hyperledger/fabric-ca-server
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: fabric-ca-orgx-pvc

