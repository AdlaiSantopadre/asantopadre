# /home/asantopadre/fabric-2.5/artifacts/fabric-tls-ca.yaml 23/01
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-tls-ca-pvc
  namespace: orgdcms
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
---
apiVersion: v1
kind: Service
metadata:
  name: fabric-tls-ca
  namespace: orgdcms
spec:
  selector:
    app: fabric-tls-ca
  ports:
    - name: https
      port: 7054
      targetPort: 7054
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fabric-tls-ca
  namespace: orgdcms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fabric-tls-ca
  template:
    metadata:
      labels:
        app: fabric-tls-ca
    spec:
      containers:
        - name: fabric-tls-ca
          image: hyperledger/fabric-ca:1.5.9
          args:
            - sh
            - -c
            - fabric-ca-server start -b boot:bootpw
          env:
            - name: FABRIC_CA_HOME
              value: /etc/hyperledger/fabric-ca-server
            - name: FABRIC_CA_SERVER_CA_NAME
              value: tls-ca
            - name: FABRIC_CA_SERVER_TLS_ENABLED
              value: "true"
            - name: FABRIC_CA_SERVER_CSR_CN
              value: fabric-tls-ca
            - name: FABRIC_CA_SERVER_CSR_HOSTS
              value: fabric-tls-ca,fabric-tls-ca.orgdcms.svc,fabric-tls-ca.orgdcms.svc.cluster.local,localhost
            - name: FABRIC_CA_SERVER_CFG_IDENTITIES_ALLOWREMOVE
              value: "true"
          ports:
            - containerPort: 7054
          volumeMounts:
            - name: data
              mountPath: /etc/hyperledger/fabric-ca-server
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: fabric-tls-ca-pvc

