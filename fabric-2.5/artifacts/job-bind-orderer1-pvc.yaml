# job di bind dei PVC di orderer1
apiVersion: batch/v1
kind: Job
metadata:
  name: bind-orderer1-pvc
  namespace: orgdcms
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: hold
          image: busybox
          command: ["sh", "-c", "sleep 300"]
          volumeMounts:
            - name: ledger
              mountPath: /var/hyperledger/orderer
            - name: msp
              mountPath: /var/hyperledger/orderer/msp
            - name: tls
              mountPath: /var/hyperledger/orderer/tls
      volumes:
        - name: ledger
          persistentVolumeClaim:
            claimName: orderer1-ledger-pvc
        - name: msp
          persistentVolumeClaim:
            claimName: orderer1-msp-pvc
        - name: tls
          persistentVolumeClaim:
            claimName: orderer1-tls-pvc
