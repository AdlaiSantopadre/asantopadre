# Pod temporaneo per caricare MSP
apiVersion: v1
kind: Pod
metadata:
  name: pvc-shell-orderer3-msp
  namespace: orgx
spec:
  restartPolicy: Never
  containers:
    - name: shell
      image: busybox:1.36
      command: ["sh","-lc","sleep 3600"]
      volumeMounts:
        - name: msp
          mountPath: /msp
  volumes:
    - name: msp
      persistentVolumeClaim:
        claimName: orderer3-msp-pvc
