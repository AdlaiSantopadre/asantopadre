TLS identity for fabric-ca-orgx.

Emitted by the unique TLS-CA.
Used exclusively as server TLS certificate for the Kubernetes Deployment
fabric-ca-orgx (namespace orgx).

Profile: tls
Type: server

Not a registrar.
Not an application CA identity.
Used only for TLS endpoint exposure (port 7054).

If lost, re-enroll using tls-ca-admin.
