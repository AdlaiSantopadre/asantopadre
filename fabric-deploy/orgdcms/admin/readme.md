OrgDCMS admin (admin dell’organizzazione Fabric).
Creato con:

register orgdcms-admin
enroll in fabric-deploy/orgdcms/admin

Questa è l’identity che firma:

peer channel join
peer lifecycle
channel update
